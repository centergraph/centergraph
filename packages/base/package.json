{
  "name": "@centergraph/base",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@centergraph/shared": "*"
  },
  "scripts": {
    "dev": "deno run -A --unstable-sloppy-imports --watch src/Base.ts",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  }
}
