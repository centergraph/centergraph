{
  "name": "@centergraph/base",
  "private": true,
  "description": "The backend of CenterGraph",
  "version": "0.0.0",
  "type": "module",
  "dependencies": {
    "@centergraph/shared": "*"
  },
  "scripts": {
    "dev-deno": "deno run -A --unstable-sloppy-imports --watch src/Base.ts",
    "dev-bun": "bun --watch src/Base.ts",
    "dev": "npm run dev-bun",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  }
}
